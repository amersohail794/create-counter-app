# ─── Phase 1: Mock Mode ───────────────────────────────────────────
# MSW intercepts all /api/* calls. No real backend needed.
# Fake SSOcookie is seeded automatically on app start.

VITE_USE_MOCK=true

# No base URL needed — MSW intercepts relative paths directly.
VITE_API_BASE=
